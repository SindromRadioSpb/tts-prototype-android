-- Этап 1 «двухрежимности» (решение владельца 2026-07-19): личнотекстовые cloze ВОЗВРАЩЕНЫ при
-- активном agent_text_grants — вместо подавления честность переезжает в ПРОВЕНАНС: задание,
-- выданное/отвеченное при живом гранте, помечается agent_exposed (агент мог видеть стимул).
-- Флаг ставится на mint (точность «грант отозван между выдачей и ответом» закрывается
-- OR-проверкой activeGrant на грейде); в review_log уезжает meta.agent_exposed — будущее
-- D1-взвешивание (успех мягче, провал полновесен) решается ПО ДАННЫМ, не сейчас. Additive.
ALTER TABLE agent_challenges ADD COLUMN agent_exposed INTEGER NOT NULL DEFAULT 0;
