# PAS слайс D — Оркестрация обучения (спека-дельта, **v2 после критики**)

**Дата:** 2026-07-12 → **v2** (адъюдикация критики wf_dd4bc294: 3 линзы, 27 уникальных находок после дедупа — 1 BLOCKER / 14 MAJOR / 12 MINOR — ВСЕ приняты; развороты помечены ⚡). **Программа:** `PREMIUM_AGENT_SYSTEM_RECON_2026_07_11.md` §2-D. **Прод на старте:** v3.11.163. Порядок имплементации: **D1 (next-text + телеметрия-substrate) → D2 (умная сессия+) → D3 (reason-aware нуджи) → D4 (⚙ настройки)** — отдельные коммиты/гейты (v3.11.164/165/166/167).

> **СТАТУС: СЛАЙС SHIPPED ЦЕЛИКОМ — v3.11.164 (D1, d8d2b01) · v3.11.165 (D2, bdbb40a) · v3.11.166 (D3, eeb845e) · v3.11.167 (D4, a6a0ed4), прод-деплой подтверждён (client-config 3.11.167), агентский live-verify 2026-07-12 (claude-in-chrome, прод-профиль).**
> **Live-verify:** D1 — дом наставника несёт все блоки в порядке (план → «📖 Что читать дальше» → письмо → TG → «⚙ Наставник» → история → конструкции); пики из живого профиля вкладки (ПК-профиль тонкий → честный 🌱 coldstart БЕЗ %-бейджей и БЕЗ LLM-кнопки — по спеке; богатый профиль владельца живёт на телефоне — 🎯/🔥 ждёт owner-верифая там); живой LLM-путь «Почему?» — openrouter/Nemotron, серверный grounding (титул из индекса, zone=hard из cov=0.21 серверной деривацией), заземлённая проза (Гордон/хаскала из карточки); usage честный (45/50 — живой день владельца). D4 — полный цикл: brief→detailed→status отражает→восстановлен brief; language='he' → 400 BAD_LANGUAGE. D2a-чип (MA/бот) и D2b-советник (нужен in-zone corpus-текст + телефонный профиль) — owner-верифай на телефоне. **R10-замер D3:** SSH-путь заблокирован авто-режимом (production-read требует ручного одобрения) — замер закрывается ПЕРВЫМ живым sweep'ом: reason в nudge_ledger (SKILL_GAP_AVAILABLE ⇔ пул непуст); ожидание по деферрал-анализу 2026-07-09 — пул мал (dictate LIVE самоистощает его с 2026-07-08), SKILL_GAP = редкий премиум-повод, это принято мандатом D3.
> **Имплементационные отклонения от v2 (все — упрощения/находки):** (1) ⚡D2a: goal деривится ИЗ select_reason на лету (format.goalLine + descriptor.goal_line/withExplanation-фолбэк) — поле pick.session_goal НЕ вводилось: select_reason уже персистирован в challenge → recovery-консистентность по построению, selectEligible в D2a не тронут вовсе (нулевой риск живому write-path; counts дропнуты как в v2). (2) D1: timeout-ветка 8с на ensureWordStates → error-состояние «Повторить» (headless-верифай вскрыл вечный спиннер при мёртвом OPFS — R4). (3) D3-гейт — отдельный файл smoke:telegram-nudge-skillgap (19/19; расширение старого nudge-smoke не потребовалось — там нет PUBLIC_BASE_URL, его кейсы нейтральны by construction). (4) agent_ux: feature=session_goal НЕ эмитится (MA-поверхность без agentUx-хелпера; чип рендерится сервер-строкой) — эмитят next_text/scaffold_advisor/mentor_settings из Зала. (5) D4: language-сегмент «Рус/Eng» (подписи локали не требуют). ⚠ Ops-наблюдение верифая: prod disk_pct_used 81% (warn; был 65% после P8.6) — вероятно, накопленные docker-образы от частых деплоев; owner-шаг: `docker system prune` по ранбуку P8.6.

**Канон-границы (чартер §7):** LLM не утверждает морфологию (R1); память ученика пишет только детерминированный путь; ни одного LLM-вызова без явного тапа; textContent-рендер; классы privacy A–D; каждый сценарий через reserveLlmCall (R16); один селектор/одно due-окно (AI_MENTOR §10); scaffold-изменения НИКОГДА молча (R11/R4).

## Grounded-факты разведки (2026-07-12, живой код; 4 зоны + верификация критики)

- **Планнер** (`agent/planner.js`): buildPlanCore → секции `fresh_struggles / production_gap / due / read`; секция read БЕЗ items — «что читать» в плане НЕТ. plan() = scenario `plan`, LLM только переформулировка, isCleanProse.
- **Каталог/вокаб корпуса на сервере НЕ читается никем.** Оба артефакта в git → в Docker-образе: `corpus-index-v<N>.json` (v7 526КБ; `ready[]` = 796 полных карточек id/title/author/era/genre/segments/text_key) и `corpus-vocab-v<N>.json` (919КБ; dict 6887 pid + works{ids,tok,m,n,ez}). ⚡Версию бампит ТОЛЬКО publish-corpus-batch в `library-ui.js:40` (CORPUS_CATALOG_VERSION); все существующие скрипты извлекают её regex'ом из library-ui.js (`build-author-nodes.js:22` и др.) — совпадение по построению.
- **Движок i+1 — клиентский и ЕДИНСТВЕННЫЙ** (`public/js/corpus-vocab.js`, pure, dual-export): coverageForWork (⚡ДВА канала честности: matchedDrillCov + totalCov/fallbackShare/loadFlag — matched-only «льстит» текстам с именами/архаикой), classifyZone (0.70–0.90 in), pickPersonalRail (MIN_RAIL=3 → kind next/challenge/null→coldstart), frontier. Рельса 🎯/🔥/🌱 + handoff + бейджи — всё через него; ⚡kind-фрейминг — контракт движка (challenge НИКОГДА не подаётся как «для тебя»; coldstart — profile-free по ez, без %-бейджа).
- **⚠ Асимметрия знания:** клиентский knownMap (note-derived overlay + manual axis) богаче серверного `getKnownWords` (только mark-axis + FSRS). Серверный ре-скоринг расходился бы с рельсой Зала (R11).
- ⚡**corpusIndex на клиенте грузится ТОЛЬКО вкладкой «Корпус»** (`loadCorpusIndex`, library-ui.js:6393); при недогруженном corpusRoot есть ловушка-дефолт v3 (:5370). `openCorpusWork(card)` (:5071) НЕ закрывает mentor-view; прецедент host `openTextAt` явно зовёт `closeMentorView()` (:2925).
- **Селектор**: `selectEligible` в `agent/reviewSession.js` (окно `REVIEW_DUE_WINDOW=50`); ⚡снапшот (items/struggleSet/exposedSet/dictCache) — ЛОКАЛЬНЫЕ переменные внутри selectEligible, наружу отдаётся только pick; ⚡бот `/review` зовёт selectEligible НАПРЯМУЮ (review.js:189), в `start()` не заходит (тот реджектит surface≠miniapp); ⚡MA-дефолт reading_first ФИЛЬТРУЕТ items до almostLapsed ДО flagship-шага (:64) — flagship-слово (обычно lapses=0) из MA-пула выпадает легитимно; ⚡dictateEligible — замыкание с ops-счётчиком dNoAsset (:82-101 → лог «run bake-dictate-audio» :152). MA-карточка УЖЕ рендерит d.explain из select_reason (miniapp-ui.js:413; default_* строк НЕ имеют by design).
- **Нуджи**: `db/nudgeRepo.js` sweep 15мин; порядок гейтов: prefs fail-closed → muted → quiet/window → claimedToday → backoff → getDue(limit:500)>0 → reason → claim-before-send; reason = recentlyActive(7д) ? DUE_READY : RETURN_AFTER_GAP; единый бюджет TG+push (`nudgeLedgerRepo`, 1/local_day); push-sweep reason всегда DUE_READY, при живой подписке почти всегда выигрывает день. Копия due-нуджа — префикс-паттерн «Готово к повторению: N.» (format.js:74 — ⚡сознательный обход русской плюрализации).
- **P7.3d owner-решения ЗАФИКСИРОВАНЫ** (recon §7b/§8e): приоритет **RETURN_AFTER_GAP > SKILL_GAP > DUE_READY** (SKILL_GAP только recentlyActive); copy с ОБЩИМ due-count, НИКОГДА не count-of-dictate (класс A); window-pin, withChannelStats захардкожен, снапшот-проброс, real-/review гейт.
- **Скаффолд Зала — клиентский** (localStorage; ⚡ruMode — ГЛОБАЛЬНЫЙ конфиг на все тексты). W5 fade-graduation: one-time full→adaptive, бар `.reader-fadegrad` (⚡спасён от mobile-ловушки `button{width:100%}` точечным `library.html:680`), ключ `room.fadeGradOffered`, гейт `fadeGraduationReady` — глобальный по профилю (не трогать).
- **P6.5 cross-text по секции плана УЖЕ есть** (`startPlanSectionTraining`).
- **Дом наставника** (`mentor-home.js`): 6 блоков; прецеденты: writing (кнопка-ленивая загрузка), consent-тогглы (optimistic + откат + «✗ Ошибка синхронизации»), usage-строка + renderStatus-refresh после LLM (runPlan finally); `<bdi lang="he">` для иврита в RU-строках (:403).
- **⚙ Настроек нет**: `POST /api/agent/profile` осиротевший; ⚡`updateProfile` СЕЙЧАС персистит language ЛЮБОЙ строкой ≤8 и goals ЛЮБЫМ JSON без валидации (`agentRepo.js:41-52`); ответ/status goals не отдают. ⚡Весь agent-стек языково БИНАРЕН `=== "en" ? en : ru` (planner/explainer/format/roleplay/writing/MA STR) — «he» нигде не различается. ⚡Same-day dedupe объяснений ключуется (sentence_id, language, kind, word) БЕЗ глубины (`getFreshExplanation`, agentRepo.js:151-165). ⚡System планнера уже содержит жёсткую длину «2-4 short warm sentences» (planner.js:202).
- **Телеметрия `agent_ux` НЕ реализована**; ⚡клиентской очереди learner_events НЕ СУЩЕСТВУЕТ ВООБЩЕ (cloud-sync.js шлёт только review_log; grep learner_events по public/ = 0) — серверный ingest есть (learnerLogRepo.ingestBatch), но поток мёртв; ⚡PAYLOAD_ALLOW валидирует только КЛЮЧИ, значения произвольны (:87-100).
- ⚡**SW**: precached = library-ui.js, mentor-home.js, corpus-vocab.js (движок, не данные); miniapp-ui.js СОЗНАТЕЛЬНО ВНЕ SW (sw.js:254 обходит /js/miniapp-*; miniapp.html без SW — свежесть через HTTP-revalidate). Новый файл в precache = добавить URL в массив, не только бамп.
- LLM-сценарии: plan/explain/explain_word/explain_followup/comprehension/study_summary/draft_retell/roleplay/writing_review; scenario-cap прецедент roleplay; ⚡explain-семья защищена от повторной траты same-day dedupe, roleplay — cap'ом; квоты 50/200; kill-switch до reserve.
- Гейты (имена подтверждены): smoke:telegram-{selector,review,cloze,dictate,nudge} · smoke:miniapp-review · smoke:web-push · smoke:reader-scaffold · smoke:corpus-vocab-engine · agent-семья · api-smoke · gate:log-hygiene · smoke:reader-parity.

---

## D0 — Архитектурные решения

**D0.1 Скоринг D1 считает КЛИЕНТ, движок ОДИН** (асимметрия знания + R11 + 919КБ вне серверного hot-path + «не дублировать движок»). Анти-«LLM-прокси» жёстко:
- Клиент шлёт ТОЛЬКО иды и числа: `{work_id ^\d{1,8}$, cov, load_flag:bool, kind ∈ {next,challenge}, frontier_pids ≤8 int>0}`. ⚡`zone` НЕ принимается — сервер сам `require('./public/js/corpus-vocab.js').classifyZone(cov)` (тот же CFG по построению). ⚡cov вне [0,1] → 400 `BAD_COV` (НЕ clamp: невозможное значение — сигнал бага, тихая нормализация запрещена [feedback_silent_empty_vs_real_empty]).
- ⚡**Версия каталога — по построению (BLOCKER-фикс):** серверный ридер резолвит N regex'ом `CORPUS_CATALOG_VERSION\s*=\s*(\d+)` из `public/js/library-ui.js` при загрузке модуля (прецедент build-author-nodes.js:22) — НИКАКИХ захардкоженных имён файлов. Гейт: server-resolved N == клиентской константе. Чек-лист skill publish-corpus-batch дополнить строкой «/api/agent/next-text/explain принимает work_id, существующий только в НОВОЙ версии каталога».
- Текстовый grounding — только серверный: title/author/era/genre/segments из index.ready; глоссы frontier через `keyingService.displayForItemKey/glossForItemKey("pid:"+pid)`. ⚡Frontier в prompt И в ответе фреймится провенансом «слова из локального фронтира профиля устройства» (`provenance:'device_profile'`) — сервер членство pid∈work НЕ проверяет (919КБ vocab не грузим) и НЕ выдаёт фронтир за серверный факт о тексте. ⚡При load_flag=true prompt получает детерминированную кавету «в тексте много имён/архаики — реальная нагрузка выше цифры покрытия».

**D0.2 Телеметрия agent_ux — ⚡эмиттер СОЗДАЁТСЯ (клиентской очереди не существует; grounded-факт v1 был ложен):**
- Клиент (Зал): модуль-хелпер `agentUx(feature, action, latencyMs?)` — in-memory очередь + батч-flush (интервал 30с / visibilitychange / cap 20) fire-and-forget `POST /api/learner/ingest` `{schema_version, keyer_version, idempotency_key:'ux-'+uuid, learner_events:[{id:uuid, type:'agent_ux', created_at_client, payload:{feature, action, latency_ms?}}]}`. Только при активной cloud-сессии; офлайн/анонимная потеря ДОПУСТИМА и задокументирована (телеметрия non-gating, чартер §6). cloud-sync.js (канон-критичный) НЕ трогается; OPFS-таблиц НЕ добавляется.
- ⚡Провенанс-огворка (R17 анти-циркулярность): agent_ux эмитят только cloud-авторизованные — метрики принятия читать ТОЛЬКО с этой оговоркой.
- Сервер: EVENT_TYPES += `agent_ux`; PAYLOAD_ALLOW += feature/action/latency_ms; ⚡validateLearnerEvent получает ПО-ЗНАЧЕНИЮ валидацию для type='agent_ux': feature ∈ {next_text, scaffold_advisor, session_goal, mentor_settings}, action ∈ {offered, accepted, dismissed, abandoned}, latency_ms int 0..600000 — иначе row rejected `payload_value:<key>` (PAYLOAD_ALLOW ключи-only — дыра контента без этого). review-факты в learner_events по-прежнему запрещены (forbidden-list цел).
- Гейт: unit валидатора (free-text feature → rejected) + smoke events-only батча до таблицы.

**D0.3 D2 «cross-text цели»:** кросс-текстовые тренировки по секциям УЖЕ shipped (P6.5). Дельта D2 = прозрачность целей серверной сессии (D2a, ⚡механика пересмотрена критикой) + scaffold-предложения (D2b).

**D0.4 D3 поверх живого reviewSession.js**; «FLAGSHIP_DUE_LIMIT» = существующая `REVIEW_DUE_WINDOW`. R10-замер flagship-пула на прод-профиле — шаг live-verify (через реальную firstFlagshipDictate, не копию логики).

## D1 — Next-text: «что читать дальше» в доме наставника (v3.11.164)

**Клиент (mentor-home.js + library-ui.js host):**
- Блок «📖 Что читать дальше» (после plan, перед writing; кнопка-ленивая загрузка, ноль расходов на mount).
- Host-capability `nextTextPicks()`: ⚡`await loadCorpusIndex()` + требует corpusRoot (ловушку-дефолт v3 НЕ наследовать — при отсутствии root честная ошибка) + ensureVocab + ОДИН снапшот word-states → pickPersonalRail → `{kind: next|challenge|coldstart, picks:[{work_id, card, title, author, cov, load_flag, frontier_pids, frontier_count}]}` (top-3; coldstart = ez-фолбэк как у домашней рельсы 🌱).
- ⚡Терминальные состояния РАЗЛИЧЕНЫ: (1) загрузка — «Собираю…» (прецедент room.explain.loading); (2) ошибка данных — «Не удалось загрузить каталог» + повторить; (3) пустой профиль → coldstart-режим (ez-пики БЕЗ %-бейджей); (4) empty-state «Отметьте несколько слов…» ТОЛЬКО когда и coldstart пуст; (5) нет capability (MA-будущее) → блока нет.
- ⚡Kind-фрейминг = контракт движка (паритет 🎯/🔥/🌱 Корпуса): три варианта заголовка/подводки ×3 локали; challenge НИКОГДА не «для тебя»; coldstart без cov-бейджа.
- Пик: `<bdi lang="he">` титул (⚡bidi, контейнер dir="auto"), автор, «≈N% знакомо» (next/challenge), кнопки «▶ Читать» и «🤖 Почему?» (только next/challenge; ⚡coldstart без LLM-кнопки — cov/zone-граундинга нет).
- ⚡«▶ Читать» = host `openCorpusPick(pick)` → `closeMentorView()` → `openCorpusWork(pick.card)` (зеркало openTextAt; live-verify кейс «открытие из mentor-view»).
- ⚡«Почему?»: client-memo ответа per work_id (Map на сессию — повторный тап показывает кеш, НЕ жжёт квоту); usage-строка в ответе + renderStatus-refresh (паттерн runPlan finally). Серверный scenario-cap НЕ вводится (1-тап сценарий, memo закрывает повтор; общая квота 50/день).
- Обогащение плана: в plan-секции `read`, если host умеет nextTextPicks — приписка «например: <bdi>title</bdi> (~N% знакомо)» top-1 пика (kind=next only), тап = openCorpusPick. Детерминированно, бесплатно.
- agent_ux feature=next_text: offered/accepted (Читать)/dismissed; why-тап → accepted c latency_ms.
- i18n `room.nexttext.*` ×3 (все kind-варианты); SW bump (mentor-home.js, library-ui.js).

**Сервер — `agent/nextText.js` + glue внутрь CLG-P6-региона:**
- `POST /api/agent/next-text/explain` (rlAgentExplain + CSRF) `{pick:{work_id, cov, load_flag, kind, frontier_pids[]}}`:
  1. Валидация: work_id ∈ index.ready (версия по построению, D0.1); cov ∈ [0,1] иначе 400 BAD_COV; kind ∈ {next,challenge}; frontier_pids ≤8 int>0; неизвестный work → 400 UNKNOWN_WORK.
  2. Grounding: карточка из index (title/author/era/genre/segments) + zone=classifyZone(cov) сервером + глоссы frontier (provenance device_profile) + learner-контекст из графа (due_now, weak count).
  3. kill-switch → 503 до reserve; `reserveLlmCall scenario='next_text'` → advisory-проза 2–5 фраз языком профиля: почему этот текст сейчас; ⚡kind=challenge → challenge-фрейминг в prompt («подача как вызов, не как комфортное чтение»); ⚡load_flag=true → детерминированная кавета в prompt; R1-guard (морфология — только переданные resolver-глоссы); isCleanProse.
  4. Ответ `{ok, text, provider, model, usage, grounding:{title, author, zone, frontier:[{pid, lemma, meaning?}], frontier_provenance:'device_profile'}}`; ничего не персистится (agent_explanations НЕ пишется — не объяснение конструкции; повтор закрыт client-memo).
- Телеметрия v1 = ledger scenario `next_text`.

**Гейт `smoke:agent-next-text`** (hermetic, mock, explicit exit): UNKNOWN_WORK; BAD_COV (1.5 → 400, НЕ clamp); frontier >8 → 400; ⚡version-by-construction (резолв == regex по library-ui.js; кейс «бампни временную копию → ридер видит новый файл»); unit buildNextTextPayload: system байт-стабилен per language, клиентские числа только в data, kind=challenge → challenge-фрейминг, load_flag → кавета, frontier помечен device_profile; kill-switch 503 без резерва; ledger ровно 1; glue-регион ассерт; agent_ux валидатор-кейсы (D0.2). Регрессия: agent-семья + api-smoke + log-hygiene.

## D2 — Умная сессия+ (v3.11.165)

**D2a «Цель сессии» — ⚡механика пересмотрена (критика: снапшот наружу не выходит, бот в start() не заходит):**
- Деривация ВНУТРИ selectEligible: возвращаемый pick получает поле `session_goal = {kind}` СТРОГО из фактически сработавшего шага: flagship → close_dictation_gap; cloze с reason=recent_struggle_prefer_cued → struggles; default_* → due_backlog. ⚡БЕЗ counts (числа из фильтрованных/нефильтрованных пулов противоречили бы унифицированным счётчикам R3.x — не лжём числами вовсе).
- ⚡Потребители: MA (miniapp-ui.js) — чип цели ТОЛЬКО когда d.explain ПУСТ (default_*-пики; при flagship/struggle explain-строка уже несёт цель — дубль на 380px запрещён, R4); бот (review.js) — goal-строка первой репликой только когда selectExplanation пуст. ⚡Таблица режимов: manual/ahead/resume/receptive_fallback → чипа НЕТ (ahead уже имеет ahead_note).
- ⚡Consumer-sweep расширения возврата (поле аддитивное, pick-содержимое байт-неизменно): review.js:189 (spread в caps — session_goal НЕ попадает в challenge-строку), reviewSession.start ×3 call-site, selectForModality не трогается. Гейт: byte-parity pick'а на фикс-сиде в ОБОИХ allocation-режимах + чип-строки ru/en в MA STR.
- Copy (без чисел): «🎧 Цель: закрыть разрыв аудирования» / «🔁 Цель: отработать недавние сбои» / «📚 Цель: разобрать очередь повторения» (MA STR ru/en; бот format.js ru/en).

**D2b Scaffold-советник (Зал) — «леса сходят ТОЛЬКО по предложению».**
- Решение — pure-модуль `public/js/scaffold-advisor.js` (dual-export; ⚡ДОБАВИТЬ URL в sw.js precache-массив, не только бамп): `advise({cfg, coverage, flags})` → предложение | null.
  - Правило N (существующее W5): niqqudMode full→adaptive; гейт `fadeGraduationReady` НЕ меняется; ключ `room.fadeGradOffered` сохраняется (ветеранов не переспрашиваем); рендер переезжает на общий бар.
  - Правило R (новое): ruMode 'show'→'reveal'; гейт: corpus-текст И zone∈{in,easy} И ⚡`!coverage.loadFlag` (второй канал честности — иначе предложим «читать по-иврит-ски» текст, где половина токенов — непокрытые имена/архаика) И ключ `room.ruRevealOffered` не стоит.
  - Приоритет N>R; 1 предложение за сессию чтения.
- ⚡Копия правила R раскрывает ГЛОБАЛЬНОСТЬ эффекта (улика локальная — эффект на все тексты): «Скрывать перевод до тапа — во всех текстах; вернуть можно в ⚙ читалки или кнопкой Вернуть»; undo-toast ttl 10с (не 5), undo восстанавливает прежний cfg + rerenderReader, offered-ключ при undo НЕ снимается (не переспрашиваем).
- Бар: класс `.reader-fadegrad` переиспользуется (уже спасён от `button{width:100%}`); [Попробовать]/[✕ Не сейчас]; НИКОГДА auto-flip. agent_ux feature=scaffold_advisor offered/accepted/dismissed (undo → dismissed).
- i18n `room.reader.advisor.*` ×3; SW bump; ⚡Playwright скриншот бара @380px (обе роли N/R).

**Гейт D2:** `smoke:scaffold-advisor` (node, pure): таблица — full+ready→N; adaptive+show+in-zone+!loadFlag→R; ⚡loadFlag=true→null; hard-zone→null; оба ключа→null; undo-семантика (ключ не снят). D2a — расширение smoke:telegram-selector + smoke:miniapp-review: goal-дескриптор по шагам селектора (flagship-сид → close_dictation_gap; struggle-сид → struggles; default → due_backlog), byte-parity pick'а, чип-подавление при explain. Регрессия: telegram-{selector,review,cloze,dictate} + miniapp-review + reader-scaffold + reader-parity.

## D3 — Reason-aware нуджи (v3.11.166) — BLOCKER/MAJOR-фиксы wf_659f597a + wf_dd4bc294

**Сервер:**
- `agent/reviewSession.js`: экспортируемая **`firstFlagshipDictate(userId, {nowMs, items?, struggleSet?, isExposed?, dictCache?})`**:
  - `withChannelStats:true` ЗАХАРДКОЖЕН; items не переданы → СВОЙ `getDue({nowMs, limit: REVIEW_DUE_WINDOW, withChannelStats:true})` (window-pin);
  - ⚡omitted-дефолты СЧИТАЮТСЯ, не пустуют: struggleSet → `recentStruggleKeySet(userId,{minFails:2})`; isExposed → `recentlyExposedSet(userId, dueKeys, nowMs)` на том же now; publicBaseUrl-гейт внутри (иначе нудж-путь без сетов обещал бы то, что /review не даст — уже В МОМЕНТ отправки);
  - ⚡eligibility-builder экстрагируется ОДИН (функция-фабрика `makeDictateEligible({isExposed, dictCache, stats})` с сохранённым ops-счётчиком dNoAsset — лог «run bake-dictate-audio» не глохнет; selectEligible и firstFlagshipDictate делят ЕЁ, никакой второй копии);
  - selectEligible зовёт её со СВОИМ снапшотом (ноль дрейфа/двойного keyingService); side-effects НЕТ (no exposure/challenge).
- `db/nudgeRepo.js`: reason-лестница (owner-приоритет): `!recentlyActive → RETURN_AFTER_GAP`; иначе `firstFlagshipDictate(userId,{nowMs}) → SKILL_GAP_AVAILABLE`; иначе `DUE_READY`. Скан СТРОГО за всеми дешёвыми гейтами (позиция: после due>0). claim пишет реальный reason.
- `agent/telegram/format.js`: ⚡`formatSkillGapNudge(count, lang)` ПРЕФИКС-паттерном (обход плюрализации, прецедент format.js:74): ru «Готово к повторению: N — часть можно проверить на слух. /review», N=1 → «Готово к повторению: 1 — можно проверить на слух. /review»; en аналогично. N = ОБЩЕЕ due (limit:500-скан), НИКОГДА не dictate-count (класс A).
- ⚡Известные ограничения ДОКУМЕНТИРУЮТСЯ в коде+споке: (1) push-sweep не трогаем (reason там DUE_READY; при живой push-подписке SKILL_GAP на TG редок — чередование каналов = отдельный owner-форк); (2) акцептанс SKILL_GAP = БОТ-путь /review (нефильтрованное окно == окно скана по построению); MA reading_first легитимно может подать не-диктант (almostLapsed-фильтр до flagship) — копия «часть можно…» мягкая, гейт-ассерт на MA-путь НЕ делается.
- Телеметрия: reason в nudge-ledger (есть).

**Гейт (расширение `smoke:telegram-nudge`):** flagship-сид → sweep reason=SKILL_GAP + текст класс-A (без HE-форм/без dictate-count) → **РЕАЛЬНЫЙ бот-путь review.startReview → createChallenge kind=dictate**; ⚡exposed-сид → DUE_READY; ⚡struggle-сид (2 провала/24ч) → DUE_READY; >REVIEW_DUE_WINDOW due, flagship на позиции 51+ (lapses-хвост) → НЕ SKILL_GAP; no-asset/has-history/reading-weak → DUE_READY; долгий гэп + flagship → RETURN_AFTER_GAP; state-drift (2 провала МЕЖДУ нуджем и /review → cued, «честен на момент нуджа»); ⚡копия N=1/2/21; withChannelStats-зубы (сид real channel_stats, реальный путь). ⚡Регрессия ПОИМЁННО: smoke:telegram-selector + telegram-review + telegram-cloze + telegram-dictate + **smoke:miniapp-review** (единственный драйвер reading_first-пути) + telegram-nudge + web-push; byte-parity selectEligible на фикс-сиде в обоих allocation-режимах.

**R10-замер (live-verify):** flagship-пул владельца через реальную firstFlagshipDictate на проде; результат в статус-блок (ожидаемо мал — SKILL_GAP как редкий премиум-повод зафиксировать честно).

## D4 — ⚙ Наставник: настройки v1 (v3.11.167)

- **Только потребляемые переключатели:**
  1. **Язык объяснений — ⚡СТРОГО ru/en** (весь agent-стек бинарен; «he» был бы переключателем-пустышкой — тем самым классом, из-за которого mode не шипится; he = отдельный форк с протяжкой через ВСЕ промпты/format/MA).
  2. **Глубина** — `goals_json.depth ∈ {brief, detailed}`; ⚡потребление = ЗАМЕНА length-клаузы system (не аппенд — конфликт с жёсткой «2-4 sentences» планнера): ДВА полных system-варианта per language (brief=текущий, detailed=новый развёрнутый), все байт-стабильны. Точки: explain/explain_word/followup/plan.
  3. mode НЕ шипится (не потребляется).
- ⚡Валидация сервера (updateProfile обретает первый вызов — и зубы): language ∈ {ru,en} → иначе 400; goals = закрытый allowlist {depth ∈ {brief,detailed}} → неизвестный ключ/значение 400 (недекларированный privacy-blob в goals_json запрещён). Ответ updateProfile и GET /api/agent/status.profile += goals.depth (read-back).
- ⚡Depth в dedupe объяснений: writeExplanation кладёт depth в body_json; getFreshExplanation-предикат += depth (back-compat: строки без depth матчатся только при effective depth='brief') — иначе смена глубины до конца дня отдаёт кеш старой (переключатель «не работает»).
- Блок «⚙ Наставник» (после telegram, перед history): сегмент-контролы language/depth; ⚡сохранение по паттерну consent-тогглов: optimistic + откат + «✗ Ошибка синхронизации» + disabled на время запроса. agent_ux feature=mentor_settings.
- Гейт `smoke:agent-profile`: POST {language:'xx'} → 400; goals {foo:1} → 400; happy → GET status отражает depth; unit: brief vs detailed → РАЗНЫЕ system, каждый байт-стабилен при повторе; ⚡интеграционный «explain@brief → смена depth → тот же якорь → НЕ dedupe» ; i18n `room.mentor.settings.*` ×3; SW bump (mentor-home.js).

## Общее

- Коммиты: D1 → v3.11.164 · D2 → v3.11.165 · D3 → v3.11.166 · D4 → v3.11.167; каждый: гейты (explicit exit) → ⚡SW: bump + precache-массив для нового scaffold-advisor.js; miniapp-ui.js ВНЕ SW (Telegram-webview без SW, свежесть HTTP-revalidate — SW-бамп для MA не нужен и невозможен) → commit+push → deploy-poll.
- Live-verify (прод-профиль владельца): Зал — блок next-text (реальные пики, kind-фрейминг, «Почему?» с usage, «Читать» ИЗ mentor-view), советник-бар @380px, ⚙-настройки (смена depth реально меняет длину объяснения); MA — чип цели на default_*-пике; TG — R10-замер flagship-пула + reason.
- Все новые строки Зала ru/en/he; MA-строки в STR ru/en; бот-copy в format.js ru/en.
- Регрессия на каждом коммите: agent-семья целиком · api-smoke · log-hygiene · reader-parity · studio-agent (не трогаем, innerHTML=3).

## Журнал адъюдикации (критика wf_dd4bc294, 3 линзы, 27 уникальных после дедупа — ВСЕ приняты)

BLOCKER version-pin серверного ридера (×2 линзы) → резолв версии regex'ом из library-ui.js по построению + гейт равенства + строка в publish-чеклист · MAJOR zone/frontier как клиентские утверждения → zone дропнут из схемы (сервер считает classifyZone сам), frontier с provenance device_profile в prompt и ответе · MAJOR два канала честности → load_flag в схеме + детерминированная кавета · MAJOR coldstart/kind → kind в схеме и рендере, coldstart без %-бейджа и без LLM-кнопки, empty-state лестница из 5 состояний · MAJOR D2a-механика невыполнима (×2) → session_goal внутри selectEligible, аддитивное поле pick, бот и MA из одного возврата, counts ДРОПНУТЫ, таблица режимов «чипа нет», дедуп с d.explain/selectExplanation · MAJOR flagship-дефолты (×2) → omitted-сеты СЧИТАЮТСЯ (real recentStruggleKeySet/recentlyExposedSet) + гейт-сиды exposed/struggle · MAJOR клиентской очереди learner_events нет (×2) → эмиттер создаётся (in-memory батч → /api/learner/ingest, cloud-only, потеря офлайн задокументирована, R17-оговорка провенанса) · MAJOR value-валидация agent_ux → по-значению enum/clamp иначе reject · MAJOR he-пустышка (BLOCKER линзы UX) → ru/en строго + enum-валидация language · MAJOR depth-dedupe → depth в body_json+предикат c back-compat · MAJOR loadFlag в правиле R → гейт += !loadFlag · MAJOR глобальный флип ruMode → копия раскрывает глобальность + undo ttl 10с + «вернуть в ⚙» · MAJOR плюрализация SKILL_GAP → префикс-паттерн + N=1 форма + гейт N=1/2/21 · MAJOR corpusIndex не грузится вне вкладки → await loadCorpusIndex + запрет v3-дефолта + 5 состояний · MAJOR openCorpusPick поверх mentor-view → closeMentorView первым · MINOR miniapp-ui «precached» ложь (×2) → формулировка исправлена · MINOR scaffold-advisor.js в precache-массив · MINOR повторная трата «Почему?» → client-memo + usage-строка + renderStatus (scenario-cap не нужен) · MINOR cov-clamp → 400 BAD_COV · MINOR bidi → bdi lang=he + dir=auto · MINOR width:100% бар → переиспользовать .reader-fadegrad · MINOR настройки без фидбэка → optimistic+откат+ошибка · MINOR depth аппенд vs замена → ЗАМЕНА length-клаузы · MINOR goals без allowlist → закрытый {depth} + 400 · MINOR MA reading_first vs SKILL_GAP (×2) → задокументировано, акцептанс=бот-путь · MINOR регрессия поимённо → miniapp-review включён · MINOR dNoAsset-счётчик → единый eligibility-builder с stats · MINOR чип-дубль с explain → чип только при пустом explain.
