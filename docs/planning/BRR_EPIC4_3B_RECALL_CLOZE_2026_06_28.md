# Эпик 4.3b — recall-loop / cloze (замыкание петли удержания) · P1 / L · вед. R2/R5/R8/R10

**Дата:** 2026-06-28 · **Статус:** ✅ SHIPPED+PROD (v3.11.26, `b3c7876`). Петля удержания замкнута. Live-verify ниже.
**Родитель:** `BRR_EPIC4_RETENTION_LOOP_2026_06_26.md` §4.3b · `BRR_EPIC4_3A_STUDY_LIST_PREMIUM_2026_06_27.md`. Память [[project_brr_ux_audit]] · [[project_srs_strategy]] · [[feedback_no_override_grounded_reading]].

## Зачем
4.3a даёт сбор/просмотр/фильтр/разметку словаря, но НЕ заставляет активно вспоминать. 4.3b = активное припоминание в реальном контексте + авто-движение статуса → moat: LinguistPro не только объясняет текст, но тренирует память на читаемом материале.

## Лучшие практики рынка (+ наш one-up)
- **Clozemaster** — cloze в реальном предложении, MC/ввод, SRS-раунды по частоте. → мы: предложение из ТЕКУЩЕЙ книги + твой перевод.
- **Anki/FSRS** — cloze-deletion + интервалы. → мы: ручные уровни l1–l4→знаю как ось SRS, отдельно от Anki-расписания.
- **Quizlet «Learn»/Memrise** — эскалация выбор→ввод по зрелости. → маппинг на l1–l4.
- **Duolingo** — сессии ~12–20, мгновенный фидбэк. **LingQ** — активности из слов урока. **Lute/LWT** — тест термина в контексте, статус 1–5.

## Ролевое исследование
- **R2 (вед.):** testing effect; тестировать ЛЕММУ в контексте; узнавание для свежих, воспроизведение для зрелых.
- **R10 (МОАТ):** дистракторы морфо-честные — та же POS, семья корня/гомограф-чтения, близкая длина, из лексики ТОГО ЖЕ текста. Невозможно в generic-приложениях без ивритского морфо-движка.
- **R1:** никуд-скаффолд (подсказка на низах, голо на верхах); ввод принимать КОНСОНАНТНО, огласовку показать на ответе.
- **R8:** контекст-родной (клоуз из читаемого текста); аудио-припоминание (🔊 reuse).
- **R11:** только уверенные (exact/likely); машинный никуд помечать; do-no-harm-полы (одна осечка не рушит знание); детерминизм (seed, без Math.random).
- **R5:** офлайн-first (всё из readerRows + офлайн-резолвер; аудио — существующий fallback). **R3:** дистракторы из rootIndex.

## Уникальные практики (moat)
1. Reading-native cloze (предложение из читаемой книги + твой перевод).
2. Морфо-честные дистракторы (корень-семья + гомографы через офлайн-резолвер).
3. Форма↔лемма (встречаешь склонённую форму → вспоминаешь лемму; на ответе openWordCard показывает разбор формы).
4. Do-no-harm SRS-lite (полы уровней; «знаю» не падает в «новое»).
5. Детерминированный shuffle (seed по индексу).

## Одобренные развилки (владелец 2026-06-28)
1. **Режим = эскалация выбор→ввод:** new/l1/l2 → MC (4 морфо-честных варианта); l3/l4/known → ввод.
2. **Набор = l1–l4 + new + редкое освежение «знаю»** (anti-forgetting, capped).
3. **Ошибка = мягко, с полами:** верно→+1 (…→знаю); ошибка→−1 шаг с полом: new→new, l1→l1, l2→l1, l3→l2, l4→l3, знаю→l4.
4. **Вход = тоггл «Список / Тренировка» в «📚 Учить».**

## Залоченные дефолты (мои, документированы)
- **Размер сессии N=12** (+ «Ещё» = следующая порция). Прогресс «k / N».
- **Ранжирование:** активные [l1–l4 по freq desc] + [new по freq desc]; инъекция до ~2 known (freq desc) на расставленных позициях (освежение); итого N.
- **Выбор occurrence:** предложение с наибольшим числом слов-токенов (лучший контекст), tie-break rowIdx asc.
- **Клоуз:** огласованное предложение (he_niqqud) с пропуском целевого слова (`____`); подсказка = гло́сс слова; перевод предложения (ru) — на ответе (не выдавать). Никуд цели скрыт (это ответ).
- **MC:** 4 варианта (огласованные), приоритет дистрактора sameRoot > samePOS+близкая длина > samePOS > любой; детерминизм (seed=item-index), ответ на позиции `idx%4`.
- **Ввод:** принять консонантно (final→medial норм.) совпадение ФОРМЫ или ЛЕММЫ.
- **🔊** на ответе (озвучить слово; во время вопроса не играем — выдало бы).
- **Reveal:** верно/нет + правильное слово (огласовка) + гло́сс + 🔊 + «Подробнее» (openWordCard) + ru-контекст + индикатор движения уровня «1 → 2».
- **Итог сессии:** «X/N верно» + сколько уровней продвинулось + «Ещё»/«Список».

## Архитектура (на коде)
- **`reader-morph` engine `collectReviewItems(mount, statesMap, opts)`** — один чанковый скан he-спанов (reuse общего `_scanWords`): per lemmaKey {lemmaKey, surface, niqqud, gloss, root, pos, freq, status(effective: states[lk]||'new'), nameSuspect, occ:[{rowIdx,wordOffset}]}; уверенные (exact|likely). Возвращает ВСЕ уверенные леммы (library-ui фильтрует сессию по статусам И использует весь список как пул дистракторов). Ключ — через общий `statusKeyForCard` (parity).
- **Pure (Node-testable) экспорты:** `buildCloze(tokens, wordOffset)` (бланк n-го слова, сепараторы сохранены) · `nextLevel(status, correct)` (полы по развилке 3) · `pickDistractors(answer, pool, n, seed)` (морфо-честный, детерминированный).
- **`collectNewWords`** рефакторится на общий `_scanWords` (DRY; smoke ловит регресс).
- **library-ui:** тоггл «Список/Тренировка» в шапке листа; сессия из readerRows (rowIdx→предложение); рендер вопроса (MC/ввод по уровню); проверка→reveal→`localDb.setWordStatus(nextLevel)`→invalidate readerWordStates+applyDecorations(перекраска)→след.; итог. Reuse `ReaderMorph.openWordCard`/`speakWord`/`tokenize`.

## Гейты / инварианты
- reader-morph smoke: collectReviewItems (status+occ+pool, statuses-фильтр) · buildCloze · nextLevel (все переходы+полы) · pickDistractors (морфо-приоритет, детерминизм, без ответа). all reader gates + corpus-vocab + i18n. @380px свет+тёмная (MC+ввод+reveal).
- Room-only; resolver-ядро/notes-autogen/parity не тронуты; статус — тот же word_status (не плодить Anki-карты). i18n ru/en/he. Bump SW+футеры+package. commit+push→Coolify→prod-verify (Node no-store)→live-verify Kapture (реальный текст: сессия строится, уровни двигаются). Volume-тест (большой текст).

## ✅ SHIPPED v3.11.26 (`b3c7876`, 2026-06-28)
- **Движок (reader-morph):** общий `_scanWords` (occ+meta; `collectNewWords` переведён на него — без дрейфа) · `collectReviewItems` (все уверенные леммы + effective-status + occ + пул) · PURE Node-tested: `buildCloze`, `nextLevel` (мягкие полы), `isMcLevel`, `pickDistractors` (sameRoot≫samePOS+len>samePOS, детерминизм).
- **library-ui:** тоггл «📋 Список / 🎯 Тренировка»; сессия (ранж + known-refresh интерлив); клоуз из readerRows (self-contained + DOM-fallback); MC(new/l1/l2)/ввод(l3/l4/known) по уровню; проверка→reveal (🔊 + openWordCard + ru-контекст + движение уровня «new → l1») → setWordStatus → перекраска; итог + «Ещё». CSS `.room-train-*`, i18n ru/en/he.
- **Гейты:** reader-morph (+5 рекалл-блоков), parity, scaffold 234, word-status, notes, context, corpus-vocab 37, i18n 226. @380px: вопрос(MC) + reveal(«new → l1»).
- **Live-verify (Kapture, прод, профиль 7034):** «הכינור» 973-айтем sibling + «חֲרוּז נִשְׁכָּח» 36 айтемов с реальными статусами (new:29/l3:3/known:2/l2:1/ignore:1); пример אָמַר l3→**typed** (эскалация), клоуз «כָּךְ [__] לְבָבִי», дистракторы — все глаголы (морфо-честно), l3→l4. Прод-маркеры v3.11.26 ✓. ⚠ мульти-таб OPFS-лок (Студия-таб) — мерить при wsCount>0.
- **Эпик 4 (петля удержания) ЗАВЕРШЁН:** 4.1+4.2+4.3a+4.3a+премиум+4.3b.

## 🔬 Аудит 2026-06-28 (конкуренты + код + роли + adversarial-verify + live) → backlog A/B/C
Полное исследование: `docs/research/epic4-3b-cloze-audit/2026-06-28/SYNTHESIS.md` (+ сырьё агентов `first-run-findings.jsonl`). Вердикт: ядро дизайна — на уровне лучших (cloze из своей книги + эскалация + морфо-дистракторы + «стена к чистому»); нашлись реальные баги корректности/честности + ценные практики к перениманию.

### Phase A — ✅ SHIPPED+PROD (v3.11.29, `7c1a58f`)
A1+A2 `buildClozeForTarget` (ключ по СКЕЛЕТУ, не HE-offset; бланк ВСЕХ копий — фикс дрейфа offset + утечки повтора, ~1% live) · A3 пре-сборка→только строящиеся в сессии («X/N» честный) · A4 `pickDistractors` дедуп по отображаемой форме + ни один вариант ≠ ответу · A5 <3 дистракторов→ввод-fallback · A6 `recollectStudy` bail при смене режима · A8 levelUps incl new→l1 · A9 локализованный «новое → 1» · A10 тренировка уважает «скрыть имена». **A7 = оставлено полное аудио строки с целевым словом (решение владельца — подсказка).** Гейт reader-morph +buildClozeForTarget/collision; parity/notes/context/scaffold/word-status/i18n зелёные.

### Phase B — ✅ SHIPPED+PROD (v3.11.30, `9162817`): B1 pickDistractors предпочитает ОГЛАСОВАННЫЕ кандидаты (нет bare-consonant-тычка; полный slot-inflection отложен) · B2 «Не знаю»/skip (MC+ввод)→reveal без угадывания, мягкий no-recall (nextLevel-false, не correct), нейтральный «— Пропущено» · B5 ввод принимает форму ИЛИ лемму, ± проклитика (ו/ה/ב/כ/ל/ש/מ). Гейты зелёные.
### Phase C1 — ✅ SHIPPED+PROD (v3.11.31, `d5bbdbb`): тап-сборка слова. 3-tier эскалация MC(new/l1/l2)→тап-буквы(l3/l4)→ввод(known). `_letterTiles` (буквы ответа +2 декоя, детерм. scramble seed=idx); build-area + тайлы (тап→собрать, тап по собранной→вернуть); проверка через B5 accepted-skeletons. A5-fallback→тап-буквы (без клавиатуры). Гейты зелёные.
### Phase C2 — ✅ SHIPPED+PROD (v3.11.32, `bfde91b`): time-based spacing. Миграция 058 (word_status +srs_due/interval/reps/lapses, аддитивно). `reader-morph.nextSrs` (PURE SM2-lite, nowMs-инъекция: верно→1д→3д→×2.3 cap 365; неверно→reset+lapse, due now). `local-db.setWordStatus` +опц. sched + перешёл на UPSERT (плоский set СОХРАНЯЕТ расписание; было INSERT OR REPLACE→стирало); `getSrsSchedule()`. `buildTrainSession` берёт DUE (никогда-не-тест / просрочено), добивает soonest-due (никогда не пусто); checkTrainAnswer пишет статус+расписание одним UPSERT. Device-local, НЕ Anki-карта. Гейты: reader-word-status (+миграция 058 + persist/preserve), reader-morph (+nextSrs), parity/notes/context/scaffold.

## 🔗 Прослеживаемость: требование (аудит) → реализация → версия → коммит → гейт
| Код | Требование (из SYNTHESIS) | Реализация | Версия | Коммит | Гейт |
|---|---|---|---|---|---|
| A1 | offset HE vs he_niqqud дрейфует → не то слово/видимая цель | `buildClozeForTarget` — ключ по скелету | 3.11.29 | `7c1a58f` | reader-morph |
| A2 | повтор цели в предложении → виден twin ответа | бланк ВСЕХ совпадающих токенов | 3.11.29 | `7c1a58f` | reader-morph |
| A3 | пропущенные (непостроенные) айтемы в «X/N» | пре-фильтр строящихся в startTraining | 3.11.29 | `7c1a58f` | runtime/screenshot |
| A4 | дубль-кнопки MC / дистрактор = ответ | дедуп по отображаемой форме + ≠ответ | 3.11.29 | `7c1a58f` | reader-morph |
| A5 | голод дистракторов (<4) | <3 → fallback в тап-буквы | 3.11.29→31 | `7c1a58f`,`d5bbdbb` | reader-morph |
| A6 | гонка списка/тренировки | recollectStudy bail при mode≠list | 3.11.29 | `7c1a58f` | runtime |
| A7 | 🔊 во время вопроса проговаривает ответ | ОСТАВЛЕНО — полное аудио (решение владельца) | — | — | — |
| A8 | levelUps не считает new→l1 | любое повышение | 3.11.29 | `7c1a58f` | runtime |
| A9 | сырые коды «l1 → l2» | локализованные метки | 3.11.29 | `7c1a58f` | runtime |
| A10 | имена в сессии/дистракторах | уважение `hideNames` в тренировке | 3.11.29 | `7c1a58f` | runtime |
| A11 | нет smoke на edge-cases | +гейты A1/A2/A4/A5 | 3.11.29 | `7c1a58f` | reader-morph |
| B1 | форма-tell дистракторов (никуд-присутствие) | предпочтение огласованных | 3.11.30 | `9162817` | reader-morph |
| B2 | нет «Не знаю»/skip (слепое угадывание) | skip→reveal+мягкий no-recall | 3.11.30 | `9162817` | runtime/screenshot |
| B5 | ввод отвергает ktiv male/chaser, проклитики | приём формы/леммы ± проклитика | 3.11.30 | `9162817` | runtime |
| C1 | ивритский ввод неюзабелен на мобиле | тап-буквы production-tier (MC→тап→ввод) | 3.11.31 | `d5bbdbb` | reader-morph + screenshot |
| C2 | нет time-based интервалов/«due today» | миграция 058 + nextSrs SM2-lite + due-aware сессия | 3.11.32 | `bfde91b` | reader-word-status (миграция 058) + reader-morph (nextSrs) |

(Полные баги/практики и их провенанс — `docs/research/epic4-3b-cloze-audit/2026-06-28/SYNTHESIS.md` + сырьё агентов.)

## ✅ АУДИТ-БЭКЛОГ A+B+C1+C2 ЗАВЕРШЁН (v3.11.29→32).

**Верификация:** каждая фаза — headless-смоуки (бутят реальный library.html + миграции вкл. 058 + движок на shipped-датасете), @380px-скрин через РЕАЛЬНЫЙ путь (study-shot → тренировка → MC/тап-буквы/«Не знаю»/мультибланк), прод-маркеры Node-fetch. **Live-Kapture (post-ship, ВЫПОЛНЕН 2026-06-28, прод v3.11.32, реальный профиль wsCount=7035):**
- **C2 / миграция 058:** `getSrsSchedule()` отрабатывает на реальной БД (колонки существуют) ✓; SRS-round-trip на синтетическом ключе — записано interval=1, **сохранено после плоского status-set** (UPSERT, не стёрто), status→l2, очищено ✓ → C2 работает end-to-end на реальной БД.
- **Движок на реальном тексте** (אָמַר, l3): утечки видимого ответа нет ✓, дистракторы (נֶאֱנַח / הַלּוֹחֵשׁ / הַנִּמְשָׁךְ) — все глаголы и **все огласованы** ✓, коллизии с ответом нет ✓.
- **Полный UI-кликтру** (реальный текст «חֲרוּז נִשְׁכָּח» в видимом ридере → «📚 Учить» → «🎯 Тренировка»): вкладка тренировки рендерит клоуз для אָמַר с пропуском, инлайн-🔊, гло́сс-нотой форма↔лемма («сказать (… обычно используется глагол אָמַר)»), переводом строки, и — т.к. אָמַר=l3 — **тап-сборкой из букв** (тайлы ר מ א ה כ ד) вместо MC. C1 + B + row-context подтверждены живьём.

## Phase D — ✅ ОДОБРЕННЫЙ ПЛАНОВЫЙ БЭКЛОГ (владелец 2026-06-28: «Включаем в план всё из v2-остатка»)

Статус: **APPROVED · PLANNED** (не SHIPPED). Все 7 пунктов v2-остатка приняты владельцем в план целиком. Реализуются после Эпика 4.3b (ядро ЗАВЕРШЕНО). Ценность («что даст по факту») сохранена дословно. Ведущие роли и зависимости — R-линзы проекта.

| Код | Требование | Что даст по факту (дословно) | Вед. роль | Зависимость / риск |
|---|---|---|---|---|
| **D1** ✅ SHIPPED v3.11.35/36 | **Полный slot-inflection дистракторов** | Дистракторы склоняются в ТУ ЖЕ граммати­ческую позицию, что и пропуск → исчезает подсказка «грамматически подходит только ответ»; MC начинает проверять ЗНАЧЕНИЕ слова, а не угадывание по форме/слоту → честнее и сложнее узнавание (моат R10 в полную силу). | R10 | офлайн slot-inflection (Pealim-парадигмы); B1 уже даёт огласованность как частичный шаг |
| **D2** | **Cross-text «due today» (нужны тела работ)** | Единая ЕЖЕДНЕВНАЯ очередь повторения по ВСЕМ прочитанным текстам, а не только по открытому → настоящая привычка интервального повторения («сегодня к повторению N слов» независимо от того, что читаешь) = петля ежедневного удержания Anki/Busuu. Требует доступ к предложениям из других работ (телам) для клоуза. | R2/R5 | C2 (миграция 058) уже даёт per-word расписание; нужен доступ к телам др. работ для клоуза вне открытого текста |
| **D3** ✅ | **Видимый due-счётчик** | Делает уже работающее (но невидимое) интервальное расписание ВИДИМЫМ: бейдж «N к повторению» → пользователь понимает, когда и почему слова возвращаются, и получает мотив вернуться → замыкает обратную связь SRS. | R4/R5 | C2 уже пишет srs_due → счётчик читается из `getSrsSchedule()`; низкий риск (UI-only) |
| **D4** ✅ | **Weakness-weighting** | Сессия взвешивается в сторону слов, которые ты чаще ПРОВАЛИВАЕШЬ (история lapse) → усилие тратится там, где память слабее → быстрее к мастерству (Quizlet/FSRS difficulty), вместо равномерного покрытия. | R2 | C2 уже копит srs_lapses → ранжирование `buildTrainSession` по lapse-весу |
| **D5** ✅ SHIPPED v3.11.38 | **Teach-before-test** | Перед ПЕРВЫМ тестом нового слова — краткий показ (форма + значение + аудио) → первая встреча перестаёт быть гарантированным провалом/угадыванием → засевает след памяти → честнее и лучше удержание новых слов. | R2/R8 | reuse openWordCard/speakWord; гейт: не засчитывать обучающий показ как recall |
| **D6** | **Reverse / listening / dictation** | Доп. КАНАЛЫ извлечения по тем же словам: RU→HE (производство), «услышал→опознал» (аудирование), «впиши услышанное» (диктант) → многомодальное кодирование углубляет память + тренирует слух/правописание, а не только узнавание-при-чтении. Переиспользует baked-аудио. | R8/R2 | reuse baked-аудио + B5 accepted-skeletons для приёма ввода; новые режимы рендера |
| **D7** | **Gamification (стрик/дневная цель)** | Внешнее давление возврата (стрик, XP, дневная цель) → двигает ЕЖЕДНЕВНЫЕ повторения (привычка — то, что вообще заставляет SRS работать) → выше удержание за счёт регулярности; в паре с due-очередью. | R5 | парная к D2/D3 due-очереди; нужно локальное хранилище стрика/цели; риск тёмных паттернов — держать «мягкой» |

Очередность (предложение, владелец решает): дешёвые-сначала на готовом C2 → **D3** (счётчик, UI-only) → **D4** (weakness-weighting, ранжирование) → **D1** (slot-inflection) → **D5** (teach-before-test) → **D7** (gamification) → **D2/D6** (нужны тела работ / новые каналы — крупнее). Каждый пункт — отдельная фаза с гейтами по образцу A/B/C (headless-smoke + @380px реальным путём + прод/Kapture live-verify).

### D3 — ✅ SHIPPED+PROD (v3.11.33, `46536e9`, 2026-06-29)
**Видимый due-счётчик — ДВА глобальных числа** «В работе: N · К повторению: M» (решение владельца после measure-before-code: на реальном профиле srs-строк 0, но 41 слово l1–l4 → строгий due-only показал бы 0 сегодня; двухчисловая форма честна И ненулевая сразу, «К повторению» наполняется по мере recall-сессий).
- **Измерение (live, real profile):** `getSrsSchedule()`=0 строк, `getAllWordStatuses()`=5124 (l1–l4=41, known 23, ignore 22, new 5038). `dueCounts` на реальных данных → `{inProgress:41, dueNow:0, nextDue:null}`.
- **Движок:** PURE `ReaderMorph.dueCounts(statusMap, schedule, nowMs)` → `{inProgress (l1–l4), dueNow (scheduled due<=now, «ignore» исключён), nextDue (soonest future|null)}` — детерминирован, nowMs инъектируется, never-tested «new» НЕ считается due (R11/R2 — бейдж не обещает больше, чем петля даёт). Гейт: smoke:reader-morph (+dueCounts детерм. тест: inProgress=4, dueNow=2 с ignore-исключением, nextDue=future, null-safe).
- **UI (library-ui):** `_dueBadgeEl`/`_paintDueBadge`/`refreshDueBadge`/`_humanizeUntil`; бейдж под «📚 Учить» (return-CTA) + в шапке листа (оба режима); рефреш на build панели/открытии-закрытии листа/смене статуса/bulk/каждом recall-ответе; в итоге сессии строка «К повторению ещё: N» или «Следующее повторение через X». Подписи через i18n (`room.morph.study.inProgress/due/dueMore/nextReview/unitDays/unitHours`), числа — JS (applyI18n не затирает).
- **CSS (library.html):** `.reader-aids-duebadge` flex-wrap + `.db-group{white-space:nowrap}` = перенос ТОЛЬКО по логической границе (инвариант #7); явный `[hidden]`-guard (author display:flex бьёт UA [hidden]); `.has-due` акцент; свет/тёмная через `--*`-vars.
- **Верификация:** все reader-гейты + i18n(226)+api-smoke зелёные; node --test без новых падений (9 pre-existing GCP/Studio, == HEAD baseline); @380px свет+тёмная (wrap + hidden-guard + has-due accent); прод v3.11.33 (Node no-store sw+footer); **live-Kapture на реальном профиле**: бейдж рендерит «В работе: 41 · К повторению: 0», has-due=false, синтет. round-trip ignore-исключён. ⚠ потребовался SKIP_WAITING + 2-й reload (SW отдавал старый reader-morph.js — урок `feedback_browser_verify_fresh_code`).

**Следующее: D4 (weakness-weighting по `srs_lapses` в buildTrainSession) — с детерминированным smoke на инъектированных lapse-данных** (реальные lapses появятся после первых recall-сессий, которые и провоцирует видимый бейдж D3).

### D4 — ✅ SHIPPED+PROD (v3.11.34, `ef3ab0a`, 2026-06-29)
**Weakness-weighting + лич-няж** (решения владельца: стабильная сортировка по lapses↓ · вес+лич-няж).
- **Движок:** PURE `ReaderMorph.rankByWeakness(items)` — СТАБИЛЬНАЯ переупорядочка по `_srs.lapses`↓, tie → ВХОДНОЙ порядок (текущий learning-перед-fresh + freq = tie-break); all-zero/empty/null → без изменений (no-op, пока нет провалов — НЕ регресс). R2-тонкость: проваленное «new» остаётся «new», но копит lapses → взвешиваем ВЕСЬ due-пул, не только l1–l4. Гейт: reader-morph (+rankByWeakness детерм. тест: lapses↓ b,e,d,a,c,f; all-zero=identity; empty/null=[]).
- **Сессия:** `buildTrainSession` → `active = rankByWeakness(learning.concat(fresh)).slice(0, N−kRefresh)` (kRefresh/known-интерлив/добивка notDue не тронуты).
- **Лич-няж (R2/аудит C6):** `LEECH_LAPSES=4` → `checkTrainAnswer` помечает `isLeech` → reveal показывает мягкое opt-in «Часто ошибаешься — 🚫 Игнорировать» (`onTrainLeechIgnore`→setWordStatus 'ignore', плоский set СОХРАНЯЕТ srs через UPSERT, repaint + refreshDueBadge); НИКОГДА не авто-игнор. CSS `.room-train-leech` (auto-width кнопка, без full-width-ловушки; свет/тёмная). i18n ru/en/he `leechHint/leechIgnore/leechDone`.
- **Measure-before-code (live):** профиль srs=3 строки (все верные) → lapses=0 → вес сегодня **инертен** (тождество, нулевой риск регресса), активируется по мере провалов. Поэтому верификация = детерм. injected-lapse smoke + live-eval (synOrder=b,e,d,a,c,f; identity; isLeech 3→false/4→true/4+ignore→false).
- **Верификация:** reader-morph(+rankByWeakness)/word-status/parity/i18n(226) зелёные; node --test 265/9 (== baseline); @380px свет+тёмная лич-няж (auto-width + «Отмечено: игнор»); прод v3.11.34 (Node no-store sw+footer+source); live-Kapture fresh-code eval подтвердил порядок+identity+leech-логику. ⚠ SW-staleness → SKIP_WAITING + 2-й reload ([[feedback_browser_verify_fresh_code]]).

**Следующее по очередности: D1 (полный slot-inflection дистракторов, R10)** — дистракторы склоняются в ту же грамм. позицию, что и пропуск (MC проверяет ЗНАЧЕНИЕ, не угадывание по форме). B1 (огласованность) = частичный шаг; нужен офлайн slot-inflection из Pealim-парадигм.

### D5 — ✅ SHIPPED+PROD (v3.11.38, `dab62ea`, 2026-06-29)
**Teach-before-test** (решения владельца: обычный счёт после показа · лёгкая панель всегда). Measure: ~65% слов текста — new-never-tested (≈половина сессии) → показ лёгкий + один тап.
- **Триггер:** `status==='new' && !item._srs && !item._taught` (никогда не тестировано). `renderTrainItem` → `renderTrainTeach`.
- **Teach-панель (пишет НИЧЕГО — R2 инвариант «показ≠recall»):** огласованная форма + гло́сс + 🔊 + слово в предложении (цель ВИДНА) + «Подробнее»(openWordCard) + «Понятно, проверь меня» → `onTrainTeachDone` (set `_taught`) → обычный счётный MC того же слова (D1-дистракторы держат честность немедленного теста). После первого теста srs появляется → показ не повторяется.
- **Live-Kapture (прод v3.11.38):** never-tested לִינָה «ночлег» → teach-панель (все элементы ✓) → «проверь» → MC того же слова (מְלוּנָה/לִינָה✓/מָלוֹן/תְּלוּנָה — D1 слот+L4 корень-семья). Гейт точен: повторно-встреченные слова teach пропускают. Гейты i18n(226)/reader-morph(library-ui грузится чисто)/parity зелёные. ⚠ Урок: live-verify training-флоу дорог на реальном профиле (дошёл до teach-айтема через 11 skip = soft-demote) → тестировать на одноразовом тексте.

**Следующее по очередности: D7 (геймификация — стрик/дневная цель, R5), затем D6 (reverse/listening), D2 (cross-text due — нужны тела работ).**

## v2-беклог (поглощён Phase D)
Все пункты ранее «v2-остатка» перенесены в **Phase D (D1–D7)** выше как одобренный плановый бэклог. Отдельно остаётся лишь не относящееся к recall: ввод с экранной ивритской раскладкой (частично закрыто тап-сборкой C1).
