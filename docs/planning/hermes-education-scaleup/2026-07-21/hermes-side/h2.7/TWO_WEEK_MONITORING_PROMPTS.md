# H2 parallel monitoring — owner ledger prompts

Window: **2026-07-24—2026-08-06**. The window runs independently from Codex sessions and does not
block G-H2-CLOSURE under the owner amendment of 2026-07-24. It is still mandatory evidence.

Do not paste transcripts, personal-text bodies, due lists, OAuth data or raw audio into the ledger.
Record only counts, minutes, typed outcomes, costs and short owner verdicts.

## After a real H2 learning loop

```text
H2 monitor, date YYYY-MM-DD:
- loop/tools used: <morphology / coverage / group corpus / W1 / Nakdan / ASR / voice loop>
- production minutes: speech <N.NNN>, writing <N.NNN>
- ASR previews: <N>; corrected: <N>
- W1 proposals: created <N>; confirmed <N>; rejected <N>
- typed failures/incidents: <none or codes, without user data>
- unwanted writes: <0 or exact incident>
- owner usefulness/readability: <1–5>
- metered cost observed: <currency+amount or NOT_AVAILABLE with account/source>
```

## Immediate stop report

```text
H2 STOP, date/time:
- condition: <unwanted write / unexpected metered cost >$0 without go /
  systematic hallucination or sycophancy / owner verdict <=2>
- affected loop/tool:
- safe action already taken:
- what remains enabled and verified:
```

Do not retry mutation or cost paths after a stop condition until the owner decides
`repair`, `disable` or `continue`.

## Day-14 closeout

```text
H2 parallel monitor closeout, 2026-08-06:
- total speech minutes:
- total writing minutes:
- ASR previews / corrected / correction rate:
- W1 created / confirmed / rejected:
- incidents / unwanted writes:
- actual metered cost by provider and envelope:
- per-loop owner ratings and disable/repair/continue verdicts:
```

The follow-up must update `EVIDENCE.md` and `STATUS.md`. It does not authorize any H3 charter.
