# H2_00 — MASTER ORCHESTRATION Горизонта 2 (карта-диспетчер)

> **BLOCKED UNTIL H1 CLOSURE.** Исполнять любой H2-промт можно только когда в STATUS.md:
> горизонт H1 = CLOSED И зафиксирован owner go (Д5, гейт G-H2-START). Нет go — СТОП.

⚠ Не рабочее задание: определяет, какой ОДИН H2-промт исполнять следующим.

## Шаги
1. Прочитай `docs/planning/hermes-education-scaleup/2026-07-21/STATUS.md`, `README.md`,
   `10_ACCEPTANCE_GATES_AND_CLOSURE.md` §3.
2. Проверь G-H2-START: H1 CLOSED + continuation evidence либо зафиксированный
   owner override с ACTIVE parallel monitoring + явный owner go Д5 (цитата/дата в STATUS).
   Не выполнено → сообщи, что именно отсутствует, и остановись.
3. Живое состояние: `git log --oneline -3`, версия package.json, `docker ps` на проде не нужен —
   но зафиксируй последний прод-деплой по STATUS/истории.
4. Рекомендованный порядок (02 §DAG + 04 §7): H2.1 морфология → H2.2 coverage → H2.3 W1-семейство
   → H2.4 Nakdan → H2.5 ASR → H2.6 voice-протокол → H2.7 closure. Технически жёсткая зависимость
   только H2.5→H2.6; остальные последовательны по рекомендации, отклонение — с обоснованием в отчёте.
5. Выведи статус-срез и следующий промт (один).

## Правила
Один слайс = одна Codex-сессия; scope не расширять; каждый H2-слайс с кодом LinguistPro обязан:
не мутировать существующие MCP-схемы (только новые инструменты), пройти смоуки + существующие
гейты, задеплоиться и прод-верифицироваться, обновить STATUS. Инварианты и ловушки — 11_HANDOFF.
