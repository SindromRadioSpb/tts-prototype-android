# Room Due Continuity — диагноз + план (owner-директивы 2026-07-11)

> **СТАТУС: ПРОГРАММА ЗАКРЫТА ЦЕЛИКОМ (R1→R4, v3.11.143–151, 2026-07-11), все слайсы owner/live-verified на прод-профиле.** Ниже — живая история слайсов; продолжение работ по ментору — `MENTOR_ROLLOUT_NEXT_2026_07_11.md`.

**Триггер:** owner live-использование — «К повторению 35 / 1 / 0 в трёх местах», «2/2 пройдено при 33 due», размеченные слова не попадают в «сегодня», тупик при живом due.

## §1 Заземлённый диагноз (все цифры — РАЗНЫЕ метрики, каждая «честна» локально, вместе — противоречие)
| Счётчик | Источник | Что считает |
|---|---|---|
| Бейдж «К повторению» (Зал) | `ReaderMorph.dueCounts` ← `getSrsSchedule()` | ВСЕ scheduled-due (расписание FSRS) |
| CTA «🔁 К повторению: N» | `getDueReviewCount` (`local-db.js:2508`) | due **С сохранённым источником** (`srs_surface IS NOT NULL`) |
| Кросс-текстовая сессия | `_buildDueSourcedItems` (`library-ui.js:1433+`) | sourced **И** предложение доступно **И** cloze собрался — иначе skip |
| Mini App home | серверный `getAgentContext.due_now` | серверные проекции (все due) |

**Корневые факты:**
1. Источник (`srs_text_key/sentence_id/order_index/surface`) пишется ТОЛЬКО recall-ответом тренера (`checkTrainAnswer` → `setWordStatus(..., source)`).
2. **`markWordStatus` (разметка уровня из текста) источник НЕ передаёт** (`library-ui.js:3253` — `null`), хотя occurrence в момент тапа известен; адаптер `opts.setWordStatus=(lk,st)` (`:3367`) не несёт occurrence — контракт reader-morph (parity-locked, у Studio СВОЙ inline-адаптер в index.html!).
3. `gradeReadingTap` тоже пишет `source=null` (`:3301`), имея `occ` в руках.
4. Seed при разметке → due **+1 день** (`manualMarkSeed`) — «вернётся через ~1 дн.» честен, но owner ожидает тренируемость сегодня.
5. Корпус-тексты (Бен-Иегуда) не в OPFS `sentences` → `getSentenceForReview` их не достанет; re-anchor для корпуса = fetch work-JSON (reader-core путь).

## §2 Директивы owner (нормативно)
1. Слова поступают **по всем корпусам** (Мои тексты + Бен-Иегуда).
2. Due-на-сегодня исчерпан → **опция продолжить словами «в работе»** (ранний повтор). ✅ **v1 SHIPPED v3.11.141** — кнопка «▶ Продолжить: слова в работе» в пустом состоянии кросс-тренировки (earliest-due first, FSRS-нативный ранний повтор; пока только sourced-слова).
3. Счётчики не должны противоречить друг другу и тренажёру (P7.3d-класс).
4. Размеченные слова должны быть тренируемы сразу (через п.2-пул — при условии сорсинга, см. R1).

## §3 План слайсов
- **R1 — source-at-mark: ✅ SHIPPED v3.11.143.** Occurrence идёт через контракт карточки reader-morph АДДИТИВНЫМ 3-м аргументом (`setWordStatus(lk, st, occ)`; occ обогащён `order_index` из `row._v3_orderIndex`) — покрыты ОБА пути разметки: карточка (`onStatusSet`) и long-press поповер (`_statpopOcc`). Хост Зала строит канонический source через `occToVerifiedSource(occ)` — **ВЕРИФИЦИРОВАННО-разрешимый** (`getSentenceForReview` до записи): корпус-occ без OPFS-предложения пишет НИЧЕГО (sourced == servable, класс противоречий §1 не растёт; корпус = R4). Запись: свежий seed несёт source в самом seed-write; уже-запланированное/восстановленное слово — backfill новым `local-db.updateSrsSource` **fillOnly** (R11 do-no-harm: разметка не churn-ит доказанный source); `gradeReadingTap` строит source из своего `occ` — latest-occurrence-wins (канон recall-пути), включая srs-only carrier-путь (P4.1). Studio parity-safe by construction: Studio вообще не передаёт `setWordStatus` в reader-morph (стрип статуса там не рендерится). Гейты: memory-canon **68/68** (+5 R1: fill-only backfill · do-no-harm · latest-wins · scheduled-only · surface-required), reader-parity ✓, reader-morph ✓.
- **R2 — serve unsourced: ✅ SHIPPED v3.11.144 + R2.1 hotfix v3.11.145** (спека v2 после adversarial-критики wf_d242bed2: 3 BLOCKER + 8 MAJOR приняты; гейты memory-canon 72/72 [+4 R2-scan] · reader-morph · reader-parity). **R2.1 (live-verify на прод-профиле owner, kapture 2026-07-11):** (a) BLOCKER — `R.tokenize` возвращает объекты `{text,isWord}`, а верификация скана стрингифицировала их → скан-ярус был мёртв и ложно травил негативный кэш (`לי` 240 substring-рядов / 0 токен-хитов) → фикс `t.isWord && t.text` + бамп ключа кэша `room.r2.scanMiss.v2`; (b) главный класс backlog'а owner = `pid:`-слова с mark-only логами (замер: 16/19; להיגרע/לגעת/יד/שבר…) → новый ярус `_r2PidEntry`: `eng.pidMap` (9279) даёт лемму+огласовку+R1-верифицированный глосс, **identity по построению** (метка ключевалась в этот pid при тапе; резолверный round-trip тут ЛОЖНО режет — живой замер: citation-форма `להיגרע` → label 'guessed'); (c) word-only label-гейт расширен `exact|function` — конфидентный класс частиц (`לי`→'function', ключ+глосс совпадают) резался зря. `בלילה#` (label 'unknown', без глосса) — остаётся честным skip для word-only (но скан с починенными токенами захилил его РЕАЛЬНЫМ предложением — identity по key-равенству). **+R2.2 v3.11.146:** tt(key, fallback)-ловушка — t() возвращает сам ключ при промахе → fallback недостижим; три новых строки добавлены в ru/en/he локали (dueUnservable с {n}-плейсхолдером, элемент без data-i18n). **LIVE-VERIFIED на прод-профиле owner (kapture, 2026-07-11): сессия 1→12 айтемов, sourced 1→8 (хилы с реальными якорями: יד/החכם/רחוק/מה/צריכה/לי/בלילה…), word-only чип рендерится, 0 ошибок консоли, негативный кэш 12 честных промахов.** Остаточный рассинхрон счётчиков (бейдж 20 / CTA 8 / сессия 12) = R3. лестница **heal-first**, все ярусы под гейтом честности:
  1. **Вывод поверхности:** самая свежая **не-аннулированная** log-строка, РЕАЛЬНО несущая `meta.surface` (mark/seed-строки его не несут — критика r11-2); иначе парс `<skel>#pos`; `pid:` без логовой поверхности → честный skip.
  2. **Re-source скан (heal):** `local-db.findSentencesForWords(needles[], totalLimit)` — ОДИН батч-LIKE-проход по `he_plain` за сессию для всех кандидатов (не 12 отдельных сканов — критика r4-2), JOIN texts `is_archived=0`. Верификация против ТОЙ ЖЕ строки, которую будет использовать будущий rebuild (`he_niqqud||he_plain`, токен: `strip(t)==needle` или проклитика-снятая — критика r4-1/r11-3), surface пишется = matched-token-skeleton (совпадение по построению). **Гейт identity (BLOCKER r11-1/r17-1):** `resolveWordLight(tskel, cz.answer).lemmaKey === d.lemmaKey` — тот же канон-кейер; невокализованное предложение дополнительно требует `label==='exact'`. Heal: `updateSrsSource` fillOnly для surface-null; **не**-fillOnly для sourced-but-dead-anchor (замена доказанно-мёртвого источника верифицированным = R11-улучшение). **Негативный кэш** сканов (localStorage, 7 дней) — мёртвый набор не съедает бюджет каждую сессию (BLOCKER r4-1); бюджет per-builder-call (due:12, ahead:6, plan-section:0).
  3. **Word-only fallback** — НЕ зависит от скан-бюджета (сверх-капа слово всё равно servable — BLOCKER r4-1): `resolveWordLight(needle)`; гейты: `card.lemmaKey === d.lemmaKey` **И `label==='exact'`** (хеджированный gloss не может быть ВОПРОСОМ — критика r17-3) **И** meaning непуст. Айтем `_wordOnly`, **`_source=null` — ПРИКОЛОЧЕНО** (синтезированный surface-only source минтил бы sourced-but-unservable ряды — критика r11-4); `cz={answer: card.niqqud||needle, segments:null}`.
  4. **Подача word-only:** read/listen → reverse-промпт + **пер-айтем чип** «без контекста…» (бар каналов не молчит о смене модальности — критика r4-4); dictate только при вокализованном `cz.answer`, иначе reverse; **type→tiles** (свободный набор отвергал бы валидные словоформы — критика r17-4); MC: slotMc ОТКЛЮЧЁН (same-root банк максимизирует глосс-коллизию), B1-дистракторы с **вето по пересечению RU-глоссов** (критика r17-5), <3 честных → tiles; невокализованный answer → tiles (анти-tell — критика r4-5). `s._effChannel` → честный channel в лог; **`meta.evidence_scope='lexeme'` (BLOCKER r17-2):** word-only узнавание НЕ защёлкивает hasProvenProduction/channelStats (CONTEXT_SUPPORTED_SCOPES уже демотирует lexeme) + `meta.word_only=1` провенанс.
  5. **Пулы/консюмеры (gate-consumers-sweep):** builder-ladder параметризован — startDueReview {scan:12} · ahead {scan:6} · **startPlanSectionTraining {ladder:false}** (сохраняет свой честный empty-state); sourced-but-fetch-failed слова падают в лестницу (dead-anchor класс перестаёт быть тупиком); ahead-фильтр `source.surface` снят; пустая ahead-сборка → честная нота вместо тихого исчезновения кнопки (критика r4-6).
  6. **Честный empty-state:** due>0 при 0 собранных → «Слова ждут повторения (N), но их пока нельзя собрать на этом устройстве…» вместо ложного «Нет слов к повторению» (критика r4-3). Счётчики НЕ трогаем (R3); heal двигает слова в sourced-класс сам.
- **R3 — унификация счётчиков: ✅ SHIPPED v3.11.147.** ОДНА цифра везде: CTA «🔁 К повторению» читает тот же `dueCounts.dueNow`, что бейдж (общий предикат due<=now + ignore-исключение == `getDueWithSource` по построению); sourced-only `getDueReviewCount` УДАЛЁН (после R2-лестницы он систематически недо-заявлял — live 8 vs 20 — и прятал CTA при полностью-несорсованном backlog'e: сам по себе класс тупика). Гарантия честности остатка перенесена на клик: несобираемое слово → R2 empty-state «нельзя собрать на этом устройстве», без тихих тупиков. Ярлыки «В работе/К повторению» уже несёт бейдж; сессия = min(servable, TRAIN_N) — кап размера сессии, не counter-метрика. Mini App home `due_now` = серверные проекции (сходится через синк — owner-подтверждено 5==5).
- **R3.1 — zombie-mark backfill: ✅ SHIPPED v3.11.148.** Live-диагноз MA-расхождения (owner-скрины): due СОШЛОСЬ (5==5); «В работе 229 vs В расписании 180» — НЕ синк-гэп (локальное расписание == серверное == 180 бит-в-бит), а **зомби-класс: 60+ слов, размеченных l1–l4 ДО P5.6** (mark-only лог, srs_due NULL) — невидимы всем очередям всех поверхностей навсегда. Фикс: идемпотентный boot-sweep `backfillZombieMarkSeeds` — каждое слово через канонический `markWordStatus`-путь (seed 'seed-manual' → oracle-clean; historic-seed → restore; повторный статус mark-строку не минтит; после первого прохода запрос пуст — постоянный self-heal). Гейт: canon-smoke **75/75** (+3: boot-seed, canonical seed row, no-reseed idempotency; same-page через тест-хук `__r31BackfillZombieSeeds` — кросс-страничная OPFS-навигация в headless тихо падает на fallback-VFS). «Сделано сегодня 64 vs сегодня 57/10» — разные скоупы by design (all-surface сервер vs локальный стрик устройства D7): не баг, ярлыки различны.
- **R3.2 — типизация счётчиков + объяснения: ✅ SHIPPED v3.11.149** (owner product-review). Единая типология из 4 метрик на ОБЕИХ поверхностях: MA home +плитка «В работе» (manual l1–l4 из getAgentContext.manual == определение бейджа Зала; counts закрытый набор; 2×2 на узких экранах; «Типы упражнений» локализованы строками кнопок режимов — «📖 Контекст ×11», не «cloze ×11»); Зал: ⓘ на бейдж-полосе → шит «Мои показатели» (5 метрик со свежими значениями + объяснение каждой; «Сделано сегодня» = новый `local-db.countTodayAllReviews` ТЕМ ЖЕ каноном, что серверный getTodayActivity — review-only, annul-excluded, локальная полночь → сходится с MA-плиткой через синк) + tooltip на 🔥. Гейты: miniapp-home 15/15 (+2), canon 76/76 (+1), reader-morph, review-session 24/24. **Live-verified kapture: шит рендерит «5 · 77 · 229 · 245 · 🔥2» с объяснениями.** Owner-verify MA-плитки — на устройстве.
- **R3.3 — all-surface стрик (owner fork 2026-07-11): ✅ SHIPPED v3.11.150.** Owner-вопрос «57≠64 — расхождение?» → живой разбор: 77 = 52 room-due-queue + 6 room-recall + 19 agent:review — «сегодня 57/10» было ПОДМНОЖЕСТВОМ (только Зал этого устройства). Owner выбрал: дневная цель и 🔥 закрываются ЛЮБЫМИ повторениями (Зал+Telegram+MA). Реализация: `local-db.countReviewsByLocalDay` (review-only, annul-excluded, локальные дни) + merged-фолд `_allSurfaceStudyDays` (recalls/available = max(девайс-леджер, счёт лога); TTL-кэш 4с + инвалидация на ответе) → ВСЕ три консюмера (бейдж streakView · summary · heatmap-календарь); streakView/канон reader-morph НЕ тронут (merge на стороне хоста — parity-safe); study_day остаётся дневником устройства (available/rest-credit). Telegram-only день теперь честно квалифицирует цель и виден в календаре. Локали+фолбэки «на всех поверхностях». Canon 77/77 (+1 bucket-кейс).
- **R4 — corpus sourcing (СПЕКА ПЕРЕСМОТРЕНА по live-заземлению 2026-07-11):** root-fact §1.5 «корпус не в OPFS» — УСТАРЕВШИЙ: `openCorpusWork` МАТЕРИАЛИЗУЕТ работу в OPFS (importBundle: texts+sentences) до открытия — R1-сорсинг и R2-скан уже покрывают открытые корпус-работы (live: 183 текста / 14 686 предложений). Реальный residual = **179 несорсованных pid-слов** (метки до R1 + R3.1-посев): их окружения ЕСТЬ в OPFS, но скан искал ЦИТАТНУЮ лемму, а в тексте — флексии. Новый R4:
  - **R4a — парадигмо-осознанный heal:** иглы pid-слова = logSurface + цитатный скелет + скелеты ячеек `pidMap.cells[*].he` (uniq, cap 12/слово); один батч-LIKE-проход (needle-cap 16→96, LIMIT 400); верификация token-in-set (± проклитика); **identity-гейт НЕ МЕНЯЕТСЯ** (`resolveWordLight(tskel, cz.answer).lemmaKey === d.lemmaKey` — вся честность в нём, live-проверен на флексиях: צריכה→pid:6018); негативный кэш пер-СЛОВО (`.v3`, 7д). **Boot-drain**: fire-and-forget sweep (бюджет 48 слов/бут, только heal без сборки айтемов) — backlog дренируется за ~4 захода.
  - **R4b — синк-каденс (owner-директива «Мои показатели синхронизированы ПК↔TG»):** fullSync триггерился ТОЛЬКО на буте/вручную → добавлены ре-синк на visibilitychange→visible и после завершения сессии (троттл 90с); после успешного pull — сброс _asdCache + refreshDueBadge (стрик/цель/сделано обновляются сразу). Паритет счётчиков ПК↔MA: due/scheduled/in_progress/today — единые каноны (R3.x) + свежесть синка (R4b); полночь: девайс-tz vs prefs-tz — совпадают для владельца, задокументировано.
  - Критика: машинерия скана/heal прошла adversarial wf_d242bed2 (R2); R4a — расширение множества игл при неизменном гейте; отдельный workflow не запускался (зафиксировано).
  - **✅ R4 SHIPPED v3.11.151 (718858c) + LIVE-VERIFIED:** два boot-дрейна на прод-профиле: несорсованных **179 → 104**, sourced 66 → **141** (+75 слов с реальными контекстами); выборочная проверка якорей 4/4 честны (חיים/שהיה-проклитика/לארך/זקוף — токен в предложении); miss-cache 21 слово честно без окружений; 0 ошибок консоли. Гейты: canon 79/79 (+2 R4a — inflected-cell heal через identity-гейт, пара צריכה→pid:6018), reader-morph. Остаток backlog'а дренируется следующими бутами (48/бут) + сессионным ladder'ом.
- Каждый слайс: спека-дельта → критика (R1 обязательно — канон-писатели) → гейты (`smoke:memory-canon`, `reader-parity`, oracle) → owner-verify.

## §4 Не делать
- Не менять seed-канон (+1д) — п.2-пул решает «сегодня» без сдвига расписания.
- Не фабриковать cloze без предложения (R11 остаётся).
- Не плодить четвёртую метрику due.
