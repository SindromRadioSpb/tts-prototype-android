# P·0 — замер живого профиля владельца (measure-before-code)

**Когда/как:** 2026-07-02 (после sign-off развилок), прод `linguistpro.kolosei.com`, Kapture-evaluate
в открытой Зал-вкладке владельца (мульти-таб прокси v3.11.74+), read-only `dbQuery` по OPFS.
Требование R10-критики (CRITIQUE_R10 §4/§9): не строить бэкфилл/сид на неизмеренных данных.
Статус: сырой снимок, не редактировался. Потребитель — скоуп P0/P2/P3 в recon-доке.

## Числа

| Метрика | Значение |
|---|---|
| `word_status` всего | **5147** |
| по статусам | new 5034 · l1 40 · ignore 22 · known 20 · l4 13 · l3 10 · l2 8 (l1–l4 = **71**) |
| строк с расписанием (`srs_due IS NOT NULL`) | **37** |
| интервалы | (0,1] — 31 · **interval=0 (пост-lapse) — 6** |
| reps | reps=1 — 31 · reps=0 — 6 |
| lapses | 0 — 19 · 1 — 12 · 2 — 5 · 5 — 1 |
| sourced (`srs_surface IS NOT NULL`, D2-очередь) | **3** |
| due сейчас (не-ignore) | 37 (все; последняя сессия 3 дня назад) |
| `srs_review_events` | **0** |
| `srs_cards` (все состояния, все entity_type) | **0** |
| `srs_attempts` | 0 |
| `events(source='anki', srs_review)` | 0 |
| `study_day` | 1 день (2026-06-29), 21 recall |
| `notes_v2 word_study` | 10002 |

## Следствия для скоупа (внесены в recon §8)

1. **Бэкфилл Студии ВЫРЕЗАН из P0** (нечего бэкфиллить: srs_review_events=0). Спека
   контент-детерминированных id для бэкфилла остаётся в доке на случай чужих профилей,
   но код P0 = только пишущие пути + бандл + кейер.
2. **P3 replay-сид схлопывается в seedFromSm2** (истории карт нет; srs_cards пуст —
   Trainer буквально ни разу не использовался: «стаб» подтверждён числом).
3. **Тайминг FSRS-перехода идеальный:** SM2-состояние Зала — 37 строк, все интервалы ≤1 дня;
   shadow-diff (P1.5) будет тривиален; ветка seed interval=0 сегодня затрагивает 6 строк.
4. Anki-инжест (P4) остаётся форвард-фичей (существующих anki-событий 0).
5. D2-очередь сегодня может служить только 3 слова (sourced=3) — ожидаемо
   (source-якорь пишется с мигр. 060 только при recall-ответах).
