# Материаловедение — задачник 2: локальный канонический корпус

Дата: 2026-08-30
Статус: `PASS / LOCAL TEXT+VISUAL CORPUS / NOT IMPORTED / NOT PUBLISHED`

Отдельный finite repair завершён в утверждённом envelope. Все 60 задач и 693
строки имеют четыре непустые учебные колонки: plain Hebrew, Hebrew с ניקוד,
транслитерацию и русский перевод. У всех строк plain Hebrew свободен от ניקוד,
а буквенный скелет plain/vocalized Hebrew совпадает.

## Результат repair

- 6/6 пакетов и 642/642 ремонтируемых строк прошли локальную валидацию;
- 488 строк приняты после provider review;
- 152 строки возвращены к source-bound legacy, когда provider нарушал
  семантику или стабильную построчную адресацию;
- 2 строки условий исправлены локально после визуального source-аудита без
  обращения к провайдеру: в задаче 5 восстановлен подпункт A (50→55 мм), а в
  задаче 19 восстановлено соответствие температур и энергий удара
  (`+50, −22, −25, −28, −75 °C` → `17, 14, 12, 8, 6 J`);
- ещё 51 строка сохранила прежний canonical PASS;
- 383 огласованные repair-строки получили детерминированную нормализацию ניקוד
  к тому же буквенном скелету без изменения plain Hebrew;
- provenance каждого маршрута записан на уровне строки.

Gemini 3.7 Flash Standard использован только в пределах точного owner approval.
Получено 9 ответов с usage receipts. С учётом одного прерванного сетевого старта
зафиксировано 10 стартов из разрешённых 12. Подтверждённая стоимость —
`$1.099227`; консервативная верхняя граница, считающая прерванный старт по
worst-case, — `$1.259607` при потолке `$2.00`. Секрет использовался только в
памяти процесса и не сохранялся. Официальная цена была сверена по
<https://ai.google.dev/gemini-api/docs/pricing>.

## Канонический пакет

- файл: `Материаловедение — задачник 2-learning.zip`;
- размер: `15,891,578` байт;
- SHA-256: `f7ef3851d1d00bae0f11343371960850d61c613e6394d8cad550a9cf9fcd8954`;
- 60 задач, 693 строки, 72 materialized source/reference assets;
- 43 задачи с 90 классифицированными visual instances;
- две последовательные сборки byte-identical;
- ZIP CRC, безопасные пути, hash/decode readback и Android-v2 schema — PASS;
- in-memory import rehearsal: 60 imported, 0 skipped, 0 errors; Studio не
  изменялась.

MuPDF выдаёт предупреждение о нечитаемом встроенном цветовом профиле исходного
PDF. Это не блокирует пакет: JPEG-рендеры повторяются байт-в-байт, декодируются,
проходят CRC и hash readback.

## Что намеренно не выполнено

В пакете нет аудио, word-level karaoke timing и решений. Все 693 строки честно
помечены как missing audio. Импорт и публикация не выполнялись. Следующими
отдельными owner gates остаются:

1. ограниченный TTS sample и утверждение voice/rate/pitch перед audio APPLY;
2. завершение уже авторизованной программы `$build-reviewed-problem-solutions`;
3. isolated import rehearsal перед записью в Studio;
4. права и publication approval перед созданием публичной immutable edition.

Машинно-читаемое доказательство: `canonical-corpus-completion-audit.json`.
