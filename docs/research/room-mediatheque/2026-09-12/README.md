# Медиатека — проверка реализации и production

Дата: 2026-09-12. Итоговый production runtime commit: `0319f4215f70e6d117a73f475c2642b7ccf505a5` (release 3.11.522). Исходная реализация: `52673149` (3.11.520).

`local/evidence.json` — машинный отчёт браузерного сценария; `local/*.png` — визуально проверенные снимки синтетического каталога, а не пользовательские материалы и не оценка качества транскрипции. Генерация: `node scripts/premium/mediatheque-browser-smoke.cjs` из корня проекта. Скрипт запускает отдельный localhost-сервер, создаёт временную БД и свежие профили Chromium; реальные права или публикации владельца не затрагиваются. Повторный прогон после исправления тёмной темы — PASS.

Сводка проверок к выкладке:
- `npm test`: 1563/1563 PASS.
- `npm run test:api-smoke`: PASS.
- `npm run smoke:reader-parity`: PASS, 37 leaf-проверок и 4 случая сборки/эталона.
- Локальный браузер: создание и включение в несколько категорий, объединение/отмена, переименование со стабильным ID, экспорт/восстановление, сохранённые представления и повторные загрузки, ограниченный размер страницы, RU/EN/HE 380px, исходные строки в читателе, неизменный review_log, публичный черновик/публикация, CSRF/anonymous isolation, снятие публикации, офлайн-загрузка и организация — PASS.

После исправлений 3.11.522: 73 релевантные проверки, API smoke и полный локальный браузерный сценарий — PASS. Полный npm test указан выше для исходной реализации.

`production/evidence.json` — PASS, 32 проверки production, включая integrity, три завершённые перезагрузки, настоящий публичный читатель, возврат в каталог, RU/EN/HE 380px и тёмную тему. Генерация: `node scripts/premium/mediatheque-live-smoke.cjs`. HTTP-запросы к production только читающие; профиль Chromium отдельный, личный видеообразец синтетический. Production PNG показывают только публичный каталог.

Kapture MCP: PASS для входа из Зала, личного видеофильтра/списка/сортировки, временной категории с отменой, настоящего YouTube-воспроизведения и точного возврата. Публичный черновик проверен через предпросмотр, отмену подтверждения публикации и undo; тестовая витрина на production не публиковалась. Журнал повторений исходного профиля до и после: 7758 записей, одинаковый SHA-256. Полная фиксация публикации проверена локально. Личные данные не входят в публичные артефакты; `private/` исключён из git. Не редактировать машинные отчёты или PNG; замечания и решения — в `docs/planning/ROOM_MEDIATHEQUE_IMPLEMENTATION_2026_09_12.md`.

Физический iPhone/VoiceOver не проверены этим прогоном.
